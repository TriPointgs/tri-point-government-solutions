# Release acceptance record

## Verified before production
- Native file preview deployment dpl_81xw2oMPNcmU9jABZ5PRGeScTtJJ completed successfully.
- All 20 Node tests passed: application validation, consent, safe URLs, CSV/HTML escaping, token signatures/expiration, origin checks, admin cookie verification and preliminary matching.
- JavaScript syntax checks passed for server, client and build scripts.
- Build generated 15 HTML pages, with 9 indexable public pages.
- Build checked exactly one H1, a unique title and a canonical URL per HTML page, and checked local links for missing files.
- Preview output intentionally uses noindex and Disallow: /.

## Not verified by this execution
The automated end-to-end HTTP submission test was blocked before it executed. No synthetic application was created by that blocked test. Therefore persistent submission, runtime private storage read/write and browser administrator-session behavior are NOT being reported as verified. Vercel's protected-preview fetch returned its authentication redirect rather than the app endpoint.

## Owner acceptance before promoting the network
1. Open /contractors/join in a browser; create a clearly marked test application using your own email. Leave optional marketing unchecked.
2. Confirm that a saved reference appears. Do not assume a submitted form succeeded without a reference.
3. Open /contractors/admin; use the owner-controlled administrator credential from Vercel project environment settings, not a public link or chat message.
4. Confirm the exact test profile appears, verify marketing remains unchecked, test a service/county filter, and inspect one CSV export privately.
5. Delete only that test application using its confirmation control and confirm it is gone.
6. Check the page on a phone, keyboard navigation and a real email address correction before a recruitment campaign.

No cold outreach, recurring email delivery, paid ads, Google indexing, keyword ranking improvement or automatic solicitation ingestion has been represented as complete.
